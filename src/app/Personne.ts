
export interface Personne {
  _id: number; // Identifiant de la personne (nombre)
  date_naissance: string; // La date de naissance de la personne (string)
  nom: string; // Le nom de la personne (string)
  prenom: string; // Le prenom de la personne (string)
  pseudo: string; // Le pseudo de la personne (string)
  sexe: string; // Le sexe de la personne (string)

}
